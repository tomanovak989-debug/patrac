{
  "name": "patrac",
  "private": true,
  "description": "PÁTRAČ — síť znovuobjevení",
  "scripts": {
    "env:firebase": "node scripts/generate-firebase-config.mjs",
    "firebase:deploy-rules": "firebase deploy --only firestore:rules,storage"
  },
  "dependencies": {
    "browser-image-compression": "^2.0.2",
    "firebase": "^11.6.0",
    "i18next": "^25.4.2"
  }
}
